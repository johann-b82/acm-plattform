fastapi==0.135.3
uvicorn==0.44.0
pyjwt==2.10.1
pydantic-settings==2.9.1
sqlalchemy==2.0.49
asyncpg==0.31.0
alembic==1.18.4
psycopg[binary]==3.2.9
pandas==3.0.2
# Liest die Lieferschein-Datei; pandas braucht die Engine als eigene Abhaengigkeit.
openpyxl==3.1.5
python-multipart==0.0.26
httpx==0.28.1
# ATR: Containerbeschriftung als Word-Dokument (ohne Vorlage, von Grund auf).
python-docx==1.2.0
# ATR: Zugriff auf den Eingangsordner (SMB), ohne Einhaengen im Betriebssystem.
smbprotocol==1.15.0
# Sensoren: SNMP v2c abfragen. pysnmp 7 hat die asyncio-API unter v3arch.
pysnmp==7.1.23
# Sensoren: die SNMP-Community verschluesselt ablegen (Fernet).
cryptography==46.0.7
# Formblätter: das Logo einbetten. openpyxl braucht Pillow für Bilder.
pillow==11.3.0
# Zeugnisse: die KI-Textbildung. Ohne API-Schluessel bleibt sie inaktiv.
anthropic==1.0.0
# Dokumentenlauf: QR-Code aufs Formblatt und aus dem Scan wieder heraus.
# pyzbar braucht zusaetzlich das Systempaket libzbar0 (Dockerfile).
qrcode==7.4.2
pyzbar==0.1.9
# Die Tintenmessung im Scan rechnet auf dem Bild als Feld. numpy kommt ohnehin
# mit pandas, steht hier aber, weil es direkt benutzt wird.
numpy>=2.0,<3
